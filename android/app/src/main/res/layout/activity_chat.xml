<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/tb_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:background="@color/tb_panel">

        <View
            android:id="@+id/dot"
            android:layout_width="10dp"
            android:layout_height="10dp" />

        <TextView
            android:id="@+id/friendNick"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:textColor="@color/tb_text"
            android:textSize="17sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/callBtn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Start a call"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:tint="@color/tb_muted"
            android:src="@drawable/ic_call" />
    </LinearLayout>

    <ScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="10dp">

        <LinearLayout
            android:id="@+id/messages"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <LinearLayout
        android:id="@+id/replyBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"
        android:padding="8dp"
        android:background="@color/tb_panel_alt">

        <TextView
            android:id="@+id/replyBarText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/tb_muted"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <ImageButton
            android:id="@+id/replyCancel"
            style="@style/HeaderIcon"
            android:contentDescription="Cancel reply"
            android:src="@drawable/ic_close" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="8dp"
        android:background="@color/tb_panel">

        <ImageButton
            android:id="@+id/imgBtn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Attach a file"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:tint="@color/tb_muted"
            android:src="@drawable/ic_attach" />

        <EditText
            style="@style/TbInput"
            android:id="@+id/input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="@string/message_hint"
            android:inputType="textMultiLine|textCapSentences"
            android:maxLines="4"
            android:importantForAutofill="no" />

        <ImageButton
            android:id="@+id/sendBtn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Send"
            android:padding="8dp"
            android:scaleType="fitCenter"
            android:tint="@color/tb_accent"
            android:src="@drawable/ic_send" />
    </LinearLayout>
</LinearLayout>
